###CentOS###
###Add repository###
## CentOS/RHEL - 7
rpm -Uvh https://yum.postgresql.org/11/redhat/rhel-7-x86_64/pgdg-redhat-repo-latest.noarch.rpm

## CentOS/RHEL - 6
rpm -Uvh https://yum.postgresql.org/11/redhat/rhel-6-x86_64/pgdg-redhat-repo-latest.noarch.rpm

##install postgresql###
#11
yum -y install postgresql11 postgresql11-server postgresql11-contrib postgresql11-libs

#10
yum -y install postgresql10 postgresql10-server postgresql10-contrib postgresql10-libs

###Directory with PGDATA###
###PATH=directory with
###postgresql.conf,pg_hba.conf, etc###

Place the below in your ~/.bashrc file.

PATH=$PATH:"<FolderWith_initdb,pg_ctl>"
export PATH
export PGDATA="<FolderNameForPostgresDatabases>"

###Add firewall###
firewall-cmd --permanent --add-port=5432/tcp
firewall-cmd --reload


